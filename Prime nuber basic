void checkPrime(N){
  int count=0;
  for (let i=1; i<=sqrt(N) ;i++){
     if(N%i==0){
       if(i==N/i){count=count+1}
       else{
         count=count+2;
       }
     }
   }
if(count==2){ptint(prime)}
else{print(Note prime)}
}
